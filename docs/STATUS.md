# STATUS — Quack & Catch

> Schnellüberblick für den Session-Start. Wird nach jedem Meilenstein aktualisiert.

**Stand:** 2026-06-23
**Aktueller Meilenstein:** **M12 (Optik-Overhaul: Gummiente-Gloss + Wasser-Politur + Subway-Surfers-Grading) ✅ — fertig & gepusht (Steps 1–3 + Review-Fixes).** Teil 3/3 (= letzter) des Nutzer-Feedback-Pakets (Plan: M10→M11→M12). **Step 1 Enten:** Gummi-Gloss via `onBeforeCompile` (Specular-Hotspot + Fresnel) auf der Toon-Basis + **Detail-Mesh** (roter Schnabel + weiße Augen, teilt instanceMatrix, KEIN instanceColor → farbecht) + rundere Geo/größerer Kopf + Rim-Bump. **Step 2 Wasser:** Fresnel-Himmel-Reflexion + Crest-Glitzer + Caustics-Schimmer + sattere Tiefe (analytische Wellen-Normale, rein im Shader). **Step 3 Grading:** Grade-Pass ersetzt OutputPass (ACES + Sättigungs-Boost + sRGB, NACH Bloom → Threshold unberührt; Renderer `NoToneMapping` im Composer-Pfad = kein Doppel-Tonemap). **Review-Fixes:** Wasser-Funkeln reduced-motion-gegated (`u_shimmerAmp`) + `'off'`-ACES entfernt. **Davor: M11 (Heilige Ente + Tier-4-Rute + Crossover + 100 Karten) ✅.**
**Vorheriger Meilenstein-Header (M11):** Step 1–4 (6. Rarität `'heilig'`, Tier-4-Rute, Wissens-Crossover, Tipp-Codex 54→100). Teil 2/3 des Nutzer-Feedback-Pakets (Plan: M10→M11→M12). **Step 1:** 6. Rarität `'heilig'` (cremeweiß + goldener Halo, `weight 6`, `baseValue 900`) + kanonische `RARITY_ORDER`; heilig in allen Raritäts-Maps (`RARITY_DEFS`, `LOOT_TABLES` inkl. neuer Tier-4-Tabelle, `speedMul/catchMul/shake/tokens`), Tier-Arrays auf Länge 5, Sparkle-Gate + `TIER_ORDER` abgedeckt. **Step 2:** Tier-4-Rute `rod-heilig` „Heilige Kirmesrute" (Preis 1200, `lineStrength 6`); Alt-Weg Gold-Rute + „Stärkere Schnur". **Step 3:** Wissens-Crossover (`rewards.crossoverChance 0.12`, `RewardSystem.rollTipTier` via `RARITY_ORDER`; Tokens rarität-gebunden). **Step 4:** Tipp-Codex 54 → **100** Karten (+46: 14/16/18/20/20/12), tieferes Claude/Claude-Code-Wissen, heilig = Geheimwissen; Faktencheck-Workflow (5 Karten präzisiert). Hardcore-Chase-Tuning per Nutzer-Wunsch. **Davor: M10 (Pause/Shop/Tuning) ✅ + M9 (Lebhaftigkeit) ✅ + M8 (Audio) ✅ + M7 (Progression) ✅ + M6 (Shop) ✅ + M5 (Codex) ✅ + M4.5 (Live) ✅.**
**Letzter Build:** grün (typecheck/lint/build/preview ✓); **`npm test` 19/19 grün**; Produktions-Build ✓. **Browser-Smoke `ok:true`** (canvas 2, 0 Konsolenfehler, 0 Page-Errors) + **`catch_test` `ok:true`** (`duckCount 18`, `aliveCount 10` bei Tier 0, Fang→Reward→Pause) + **reduced-motion-Probe** (`u_shimmerAmp=0`, 0 Fehler). In-Game-Shots gesichtet: Gummi-Gloss, weiße Augen + roter Schnabel rarität-unabhängig, Wasser-Fresnel/Glitzer, ACES-Sättigung. (Hinweis: Prod-Preview zeigt das bekannte intermittente swiftshader-`MeshBasicMaterial VALIDATE_STATUS`-Rauschen am unveränderten Outline-Material — kein M12-Regress; Dev-Smoke ist 0 Fehler.)
**Code-Review (M12-Diff, 5 Dimensionen, adversarisch je verifiziert):** **2 echte Findings, beide behoben.** (1) MEDIUM: Crest-Glitzer (zeitanimiert, bloom-pumpend) hatte kein reduced-motion-Gate → `u_shimmerAmp` (balance), bei `prefersReducedMotion()` = 0 (ruhiges Specular, Fresnel/Spec bleiben). (2) LOW: `'off'`-Fallback setzte Renderer-ACES, das rohe Wasser-`ShaderMaterial` bekommt aber kein tonemapping/sRGB → Divergenz; ACES dort entfernt (`'off'` ist kein Auto-Default). Gummi-Gloss ist statisch (kein Zeit-Term) → unkritisch.
**Session-Abschluss (2026-06-23, M12):** **M12 (Gummiente-Gloss + Wasser-Politur + ACES/Sättigungs-Grading) komplett** — 4 Commits (3 Steps + Review-Fixes), Gate grün, Code-Review 2 Findings behoben, Doku aktualisiert, alles gepusht. **Damit ist das 3-teilige Nutzer-Feedback-Paket (M10→M11→M12) abgeschlossen.** Nur noch optionale Reste offen (mehrere Becken/Themes · DE/EN-Toggle · `lil-gui`). _Vorheriger Abschluss: M11 (Heilige Ente + Tier-4-Rute + Crossover + Codex 54→100) komplett._
**Politur (nach M8):** Snap-Feedback — reißt die Linie an einer zu schweren (leuchtenden epic/legendary) Ente, kommt jetzt ein eigener `snap`-Sound + Toast „💪 Zu schwer — stärkere Rute im Shop!" (`ui/Toast`); klärt das Rod-Gating auf. Verifiziert (Playwright: Snap→Toast, Auto-Hide, Miss→kein Toast, 0 Fehler).
**Live-URL:** **https://quack-and-catch.vercel.app** (Vercel, Prod-Deploy ✓ — lädt sauber, 0 Konsolenfehler, `canvas:2`; **Git-Auto-Deploy von `main` aktiv**)
**Repo:** https://github.com/Scholzer0303/quack-and-catch

## ✅ Erledigt
- **M0:** Git/Repo/origin/Push-Test, Projekt-`CLAUDE.md`, Planungsdokumente, Vite+TS(strict)+Three-Scaffold (deploy-ready).
- **M1:** First-Person-Szene — Core (Renderer/Scene/Kamera/Loop/Game), EventBus, Balance-Config, Utils (math/rng), Welt (Stand, ovales Becken + animiertes Wasser-Shader, Angel/Haken), 8 Enten als InstancedMesh auf Oval-Bahn. Visuell verifiziert.
- **M2:** Hak-Mechanik — `InputSystem` (Pointer Events), Kamera-Aim-Schwenk im Cone, `HookRaycaster` (Ray-Sphere ab Haken), `FishingRod`-State-Machine (Halten-Laden/Loslassen, Timing-Window + Perfect), Reel-Animation + `removeAndRespawn`, `ui/Reticle` (Timing-Feedback) + Hover-Highlight, `lineStrength`-Gate. Fang-Loop, Perfect & Softlock-Schutz per Interaktionstest verifiziert.
- **M3:** Belohnung + HUD + Screens — `data/ducks.ts` (Raritäten + Loot-Tables + per-Instanz-Farben), `data/tips.ts` (12 geprüfte Karten), `RewardSystem` (Tokens + Perfect-Bonus + Tipp) + `Economy` (Saldo + Unlock-Set + Bonus), `GameStateMachine` (Phasen + Rundentimer + Score), `ui/UIRoot`+`HUD`+`StartScreen`+`CardReveal` (blockierendes Tipp-Modal, Pause) + `SummaryScreen`. Core-Loop schließt: Fang → Tokens + Tipp → Timer → Summary.
- **M4:** Save + Deploy-Check — `types/state.ts` (SaveData + `createDefaultSave`), `systems/SaveSystem` (localStorage: versioniert, debounced, korruptionssicher; Flush bei `pagehide`/Tab-Wechsel), `Economy.snapshot()/hydrate()`, Verdrahtung in `Game` (Laden nach UIRoot → HUD zeigt geladenen Saldo), README ausgebaut, dispose-Audit. Tokens + freigeschaltete Tipps überleben Reload; defekte Daten → sauberer Default.

- **M4.6 (Steps 1–10 ✅ gepusht):** Direktes Fadenkreuz, heller Comic-Tag, Toon-Cel-Shading + schwarze Outlines auf Enten, **Steuerungs-Redesign**, **Fang-Engine-Neumodell** (feste Schräg-Sicht aufs ganze Becken; Maus → Wasserpunkt W; Halten senkt Haken **ins Wasser** bei W; **räumlicher Fang** mit Ente ≤ catchRadius, Perfect = mittig; Rute schwenkt stark; Rute/Haken Toon+Outline), **Schwierigkeit je Rarität** (`catchMulByRarity`), **Jahrmarkt-Welt** (Budenreihe + Wimpel-/Lichterketten + Riesenrad/Zelt-Fernkulisse, alles Toon+Outline; schlanker Holz-Plankenrand), **Juice + Bloom/Glow** (Splash · Catch-Pop · Perfect-Flash · Mini-Screenshake (skaliert nach Rarität/Perfect) · HUD-Count-up · Bloom-Postprocessing mit Mobile-Quality-Guards · Glow seltener Enten; reduced-motion respektiert), **Tipp-Modal-Politur** (Emoji-Medaillon je Tipp · Rarität-Glow/Theming via `data-rarity`+`--qc-accent` · Token-Count-up · Rarität-/Kategorie-Chips · Summary-Emoji; reduced-motion gated), **Intro-Sequenz** (3-Schritt-CSS-Storyboard Bude → Ticket → Angel → los, mit „Überspringen"; `IntroScreen` ersetzt `StartScreen`, keine neue Phase, kein Save-Eingriff).

- **M5 (inhaltlich ✅ gepusht):** Tipp-Codex — `data/tips.ts` 12 → **54 geprüfte Karten** (9 Kategorien, alle Tiers; bestehende 12 IDs stabil); neuer `ui/CodexScreen` (Phase `codex`): Tier-gefärbtes Grid, freigeschaltet → Icon + Titel + Klick-Detail, gesperrt → nur 🔒 + Tier-Farbe, Kategorie-Filter, Fortschritt; Einstieg aus Intro/Summary via `onOpenCodex`/`onCloseCodex` (Game `codexReturn` → reset-frei zurück). SaveSystem schneidet Unlock-IDs automatisch gegen die größere `TIPS`-Liste (kein Schema-Bump).

- **M6 (✅ gepusht, reviewt):** Upgrade-Shop — `data/rods.ts` (4 Ruten + 4 stapelbare Upgrades; Starter spiegelt `BALANCE.hook`-Basiswerte → regressionsfrei); `Economy` Kauf/Equip/Upgrade-Stacking (`buyRod`/`equipRod`/`buyUpgrade`/`getActiveRodStats`); neuer `ui/ShopScreen` (Phase `shop`): Ruten Kaufen/Ausrüsten/Ausgerüstet + Affordability-Dimming, Upgrades Stufe x/max, Stat-Chips; Einstieg Intro+Summary reset-frei via `shopReturn`. Rod-Stats wirken (`reach`/`castSpeed`/`reelSpeed`/`lineStrength`/`magnetRadius`/`luck`; `timingWindowMul` entfernt). SaveData additiv (`ownedRodIds`/`equippedRodId`/`upgradeStacks`, kein Schema-Bump). Review-Fixes: Glück re-rollt den sichtbaren Pool sofort + HUD-Rod-Chip folgt der Rute.

- **M7 (✅ gepusht):** Progression koppeln — `rod:statsChanged` trägt `tier` (einzige Emit-Stelle `Economy.emitStatsChanged`). `DuckSpawner` allokiert auf max. Kapazität (`Math.max(...duckCountByTier)` = 14) und steuert per neuem `setTier(tier)` die aktive Entenzahl (geparkte Slots = `alive=false` + Null-Skala-Matrix), Rotation (`rotationSpeedMulByTier`) und Loot-Table (`rollRarity(rng, tier, luck)`); `Game` ruft im `rod:statsChanged`-Handler `setLuck` **und** `setTier`. Behebt den Boot-Bug (gespeicherte Rute griff nur für `luck`). Magnet + Legendary-Gating bereits durch M6 abgedeckt → kein neuer Code, nur verifiziert. `catch_test` prüft jetzt `aliveCount` (8) statt Pool-Länge (14). Kein Schema-Bump, keine neuen Magic Numbers.

- **M8 (✅ gepusht):** Juice + Audio — neuer **`systems/AudioManager`** (prozeduraler WebAudio-Synth, Chiptune; Sound-Daten als `const` im Modul, nur `masterGain` in `balance.audio`; Lazy-`AudioContext` bei erster Geste → `audio:unlocked`, headless-sicher). Neues Event **`audio:muteChanged`** als Mute-Single-Source-of-Truth: **`ui/MuteButton`** (oben rechts, alle Phasen) emittiert, AudioManager mutet den Master-Gain, SaveSystem persistiert (`save.muted` greift; Lade-Emit vor dem Abonnieren → kein redundanter Write). Rest-Juice: **`fx/SparkleFx`** (Gold-Burst bei epic/legendary, additive HDR-Farbe → Bloom, deterministische Partikel) im bestehenden `hook:result`-Subscriber + **`fx/haptics`** (`navigator.vibrate`, gated). `lowTick` nur bei Sekundenwechsel in der Warnzone. reduced-motion: Sparkle/Haptik aus, Audio bleibt. Kein Schema-Bump.

- **M9 (✅ gepusht, Sub-Schritte 1–7):** Lebhaftigkeit, Sog & Politur. **9.1** Zuschauer-Menge (`world/CrowdBuilder`+`CharacterFactory`); **9.2** reaktive Welt (Riesenrad-Drehung + Birnen-Puls/Flash, `StallBuilder` Refactor: drehbare Teile aus dem Merge + `update`/`flash`); **9.3** Himmel-Deko (`fx/SkyDecoFx`, Ballons+Vögel, NDC-platziert); **9.4** Abend-Stimmung (Verlaufshimmel via CanvasTexture + warmer Ambient + mehr Bloom, `SceneManager`/`balance.render`); **9.5** Combo/Streak (`systems/ComboSystem` → `combo:changed`, Token-Multiplikator in `RewardSystem`, HUD-Badge); **9.6** persistenter Highscore (`systems/HighscoreSystem` → `highscore:changed`, SaveData additiv, Summary-Rekord); **9.7** Politur (Vitest+jsdom 13 Tests, `:focus-visible`, Ring-Farbe → `balance.aim`). Alle Tunables in `balance.{crowd,skyDeco,combo,stall,render}`. Kein Schema-Bump.

- **M10 (✅ gepusht, Steps 1–3):** Nutzer-Feedback nach Live-Test. **Pause-Menü** (Phase `pausemenu`, `ui/PauseScreen`, Pause-Button + ESC, `GameStateMachine.endRound`, Reset-Vertrag via `isPauseState`); **Shop-Preis-Eskalation** (`Economy.getUpgradePrice` = Basispreis × `shop.upgradePriceGrowth`^Stacks; Button/Affordability nutzen ihn; Vitest-Test); **Fang-Tuning** (`duck.speedMulByRarity` live in `DuckSpawner.update` + `duckCountByTier` → 10/12/14/16). Kein Schema-Bump. Teil 1 von 3 (M11 Heilige Ente + 100 Karten, M12 Optik folgen).

- **M11 (✅ gepusht, Steps 1–4):** Teil 2/3 des Feedback-Pakets. **Rarität `'heilig'`** (cremeweiß + goldener Halo, `weight 6`/`baseValue 900`/`catchMul 0.14`/`tokens [200,300]`) + kanonische `RARITY_ORDER` (`types/domain.ts`); heilig in allen Raritäts-Maps (`RARITY_DEFS`, `LOOT_TABLES` + neue Tier-4-Tabelle, `speedMul/catchMul/shake/tokens`), Tier-Arrays auf Länge 5, Sparkle-Gate (`Game.ts`) + `TIER_ORDER` (aus `RARITY_ORDER` abgeleitet). **Tier-4-Rute** `rod-heilig` „Heilige Kirmesrute" (Preis 1200, `lineStrength 6`); Alt-Weg Gold-Rute + „Stärkere Schnur". **Wissens-Crossover** (`rewards.crossoverChance 0.12`, `RewardSystem.rollTipTier` via `RARITY_ORDER`; Tokens rarität-gebunden). **Tipp-Codex 54 → 100** (+46: 14/16/18/20/20/12 je Tier), tieferes Claude/Claude-Code-Wissen, heilig = Geheimwissen; bestehende 54 IDs stabil. Faktencheck-Workflow (5 Karten präzisiert), Code-Review 0 Defekte. Kein Schema-Bump (`KNOWN_TIP_IDS` leitet neue IDs ab).

- **M12 (✅ gepusht, Steps 1–3 + Review-Fixes):** Teil 3/3 (= letzter) des Feedback-Pakets. Optik-Overhaul, Toon-Basis bleibt. **Enten:** Gummi-Gloss via `onBeforeCompile` (Specular-Hotspot + Fresnel-Tint) auf der geteilten `MeshToonMaterial`; neues **Detail-Mesh** (3. InstancedMesh, teilt instanceMatrix by reference, KEIN instanceColor → roter Schnabel + weiße Augen über alle Raritäten farbecht); rundere Geo + größerer Kopf + Rim-Bump. **Wasser:** analytische Wellen-Normale → Fresnel-Himmel-Reflexion + Crest-Glitzer + Caustics-Schimmer + sattere Tiefe (rein im Shader, Mobile-sicher). **Grading:** Grade-Pass ersetzt OutputPass (ACES + Sättigungs-Boost + sRGB, NACH Bloom → Threshold unberührt, Renderer `NoToneMapping` = kein Doppel-Tonemap). **Review-Fixes:** Funkeln reduced-motion-gegated (`u_shimmerAmp`), `'off'`-ACES entfernt. Kein Schema-Bump, alle neuen Tunables in `balance.{duck.gloss, basin.water*, quality.grade*}`.

## ✅ M4.6 abgeschlossen (Steps 1–11)
Direktes Fadenkreuz · heller Comic-Tag · Toon+Outline-Enten · Steuerungs-Redesign · räumliche Fang-Engine · Schwierigkeit je Rarität · Jahrmarkt-Welt · Juice+Bloom/Glow · Tipp-Modal-Politur · Intro-Sequenz. Reviewt (11 Findings, kritische behoben).

## ⏭️ Nächster Meilenstein — keiner offen (Feedback-Paket M10→M11→M12 abgeschlossen)
- **M12 ✅** abgeschlossen — damit ist das gesamte 3-teilige Nutzer-Feedback-Paket (Plan: `~/.claude/plans/pipeline-soweit-erstmal-fertig-calm-kay.md`) fertig. **Wartet auf Nutzer-Richtung** für den nächsten Schritt.
- **Optionale Reste** (nie auf Kosten der Stabilität): mehrere Becken/Themes · DE/EN-Sprachtoggle · `lil-gui` (dev-only). Reine Politur-Ideen, keine Pflicht.
- **Optional manuell durchspielen:** heilige Ente / Crossover / Codex-Vollansicht (Daten + Loop laden bereits fehlerfrei); M12-Optik live auf Desktop + Mobile (DevTools-Touch) durchklicken.
- **Neuer Kontext?** Zuerst [`docs/HANDOVER.md`](HANDOVER.md) lesen.

## 📌 Offene Punkte / Entscheidungen
- Keine offenen Blocker. Codex-Inhalt: eigene, geprüfte Karten (~50–60 in M5; 12 seit M3), siehe DESIGN.md.
- Persistenz steht (M4): SaveSystem speichert Tokens + Unlock-Set in `localStorage`. Spätere Felder (Rod/Stats/Settings) werden additiv bei gleicher `schemaVersion` ergänzt; `muted` ist reserviert, greift ab M8-Audio.

## Verifikations-Checkliste (Definition of Done je Meilenstein)
- [ ] `npm run typecheck` grün
- [ ] `npm run lint` grün
- [ ] `npm test` grün (Vitest, seit M9)
- [ ] `npm run build` + `npm run preview` fehlerfrei
- [ ] Browser: null Konsolenfehler, ~60 fps, Desktop + Mobile
- [ ] committet + zu `origin` gepusht, STATUS aktualisiert
